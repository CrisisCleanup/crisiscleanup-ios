# vim: set ft=ruby:
# frozen_string_literal: true

git_url('git@github.com:crisiscleanup/certificates.git')
git_branch('ios')
storage_mode('git')

type('development') # The default type, can be: appstore, adhoc, enterprise or development

app_identifier(['com.crisiscleanup.prod', 'com.crisiscleanup.dev', 'com.crisiscleanup.earlyaccess'])
username(ENV['MATCH_USERNAME']) # Your Apple Developer Portal username

# For all available options run `fastlane match --help`
# Remove the # in the beginning of the line to enable the other options

# The docs are available on https://docs.fastlane.tools/actions/match
